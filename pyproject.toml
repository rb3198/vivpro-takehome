[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name="vivpro"
version="0.0.1"
dependencies = [
    "fastapi[standard]",
    "uuid6",
    "bcrypt"
]